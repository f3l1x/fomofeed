# dependencies
node_modules

# cache (regenerated on CI)
cache/

# archive (persistent historical data)
archive/

# output (only public/feeds/ is tracked — see public/.gitignore)
web/.astro
web/dist

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
